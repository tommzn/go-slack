name: Go Package Release
on:
  push:
    branches:
      - main
      - 'features/**'
      - 'dependabot/**'
  pull_request:
    branches:
      - main
      - 'features/**'
      - 'dependabot/**'

permissions:
  contents: write

jobs:
  go-auto-ci:
    uses: tommzn/github-ci/.github/workflows/go.pkg.auto-ci.yml@v1.0.105
